{
  "name": "lyse",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "fmt": "bunx --bun prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.3.3",
    "vite": "^5.4.8",
    "vitest": "^2.1.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
